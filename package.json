{
  "name": "reown-server",
  "private": true,
  "dependencies": {
    "@prisma/client": "^6.3.0",
    "prisma": "^6.3.0"
  },
  "scripts": {
    "postinstall": "npx prisma generate --schema=api/prisma/schema.prisma",
    "build": "npx prisma generate --schema=api/prisma/schema.prisma"
  }
}
